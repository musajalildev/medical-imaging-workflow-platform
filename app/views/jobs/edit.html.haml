.row.justify-content-center.job-shell
  .col-sm-10.col-lg-8
    .card.job-card
      .card-header
        %span.job-form-section__eyebrow Edit job
        %h1.h4.mb-1 Update job details
        %p.text-muted.small.mb-0 Keep the request details current and replace input files if needed.
      = render 'form'